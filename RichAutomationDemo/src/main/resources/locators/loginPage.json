{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "LoginPage",
  "type": "object",
  "properties": {
    "usernameField": {
      "type": "string",
      "description": "CSS selector for the username input"
    },
    "passwordField": {
      "type": "string",
      "description": "CSS selector for the password input"
    },
    "loginButton": {
      "type": "string",
      "description": "CSS selector for the login button"
    }
  },
  "required": ["usernameField", "passwordField", "loginButton"]
}